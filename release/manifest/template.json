{
  "module": "github.com/bytechainx/configx",
  "version": "v0.1.0",
  "commit": "{{COMMIT}}",
  "tree_state": "{{TREE_STATE}}",
  "checks": {
    "fmt": "{{CHECK_STATUS}}",
    "vet": "{{CHECK_STATUS}}",
    "lint": "{{CHECK_STATUS}}",
    "unit_test": "{{CHECK_STATUS}}",
    "race_test": "{{CHECK_STATUS}}",
    "contract": "{{CHECK_STATUS}}",
    "secret_scan": "{{CHECK_STATUS}}",
    "security": "{{CHECK_STATUS}}",
    "integration": "{{CHECK_STATUS}}"
  },
  "contracts": [
    { "path": "contracts/config.schema.json", "sha256": "{{CONFIG_SCHEMA_SHA256}}" },
    { "path": "contracts/error.schema.json", "sha256": "{{ERROR_SCHEMA_SHA256}}" },
    { "path": "contracts/manifest.schema.json", "sha256": "{{MANIFEST_SCHEMA_SHA256}}" }
  ],
  "dependencies": [
    { "path": "github.com/bytechainx/configx", "main": true }
  ],
  "tools": {
    "go": "{{GO_VERSION}}",
    "golangci-lint": "{{GOLANGCI_LINT_VERSION}}",
    "govulncheck": "{{GOVULNCHECK_VERSION}}"
  },
  "artifacts": ["release/manifest/latest.json"],
  "notes": {
    "breaking_changes": "none",
    "known_risks": []
  }
}

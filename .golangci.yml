version: "2"

run:
  timeout: 5m

linters:
  enable:
    - govet
    - ineffassign
    - staticcheck
    - errcheck
    - gosec
    - unconvert
    - unparam
    - misspell

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
